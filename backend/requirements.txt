# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==23.0.0
python-multipart==0.0.9

# Settings
pydantic-settings==2.4.0
pydantic==2.8.2

# Supabase
supabase==2.7.4

# JWT verification
PyJWT==2.9.0

# ColPali visual embeddings
byaldi==0.0.6
torch==2.4.1
torchvision==0.19.1

# FAISS vector search (CPU — no GPU needed)
faiss-cpu==1.8.0

# PDF rendering + text extraction
pdf2image==1.17.0
Pillow==10.4.0
pdfplumber==0.11.4

# Gemini (free: 15 req/min, 1M tokens/day)
google-generativeai==0.7.2

# Hybrid retrieval — BM25
rank-bm25==0.2.2

# Reranking — cross-encoder (runs on CPU, ~22MB download)
sentence-transformers==3.1.1

# Evaluation metrics
scikit-learn==1.5.2

# LangSmith observability (free: 1000 traces/month)
langsmith==0.1.120

# HTTP client
httpx==0.27.2

# Utilities
python-jose[cryptography]==3.3.0